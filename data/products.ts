import type { Product } from '@/types/product';

// Source of truth for the catalog through Phase 2-3 (docs §3.4).
// Empty until the client supplies the 5 models' imagery and copy — docs §7
// open question 0. The catalog pages themselves are a separate Phase 1 slice.
export const products: Product[] = [];
